{
  "name": "bench",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "# general commands": "--------------------------------------------------",
    "clean": "shx rm -rf dist/*",
    "lint": "eslint --fix \"*.js\"",
    "test": "favicons-compiled -i icon.png -c config.json -o dist/",
    "#": "-------------------------------------------------------------------"
  },
  "dependencies": {
    "favicons-compiled": "*",
    "favicons-compiled-cli": "*"
  },
  "private": true
}
